#include<stdio.h>
int main()
{
    // array is a data structure that contains continues and similar data types.
    // the size of an arry one's declare that can not be changed.
    // array give the fastest acess time, we can directly use an index number inside the squre bracket to reach the location.
    // arrayes are very difficult to modify.
    // there is no direct indication of the ending an array, so we should commonicate the size fo the array every where in the relivante code.

    int num[10];
    for(int i=0;i<=10;i++){
        num[i]=20;
    
    }
    
    return 0;
}