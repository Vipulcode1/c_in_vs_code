// the modern programing languages like c++ java arrrange the memory block of N DIMENSional array linearly memory .the location of any cell in any dimension is done by the compiler using index number.
// a one dimensional array of size 5 has starting adress 100. the size of one memory is 1 byte.find the address of num 3.
// desired address=b+w(i-L);
// b= starting address
// w=word size
// i=
// in an array found out the address location of arr[-2],the aray is specified as [-5:1] the starting add is 150 and the word size is 2byte
